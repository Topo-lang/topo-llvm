[project]
name = "ir_embedding"

[topo]
root = "topo/main.topo"

[build]
sources = ["src/*.cpp"]
include = ["include", "../../../topo-lang-cpp/runtime/include", "../../runtime/include"]
standard = "c++17"
output = "jit_forced"
embed_ir = true
link_libs = ["topo-jit"]
