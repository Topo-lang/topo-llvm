[project]
name = "pipeline"

[topo]
root = "topo/main.topo"

[build]
sources = ["src/*.cpp"]
include = ["include", "../../../topo-lang-cpp/runtime/include", "../../runtime/include"]
standard = "c++17"
output = "pipeline_base"
link_libs = ["topo-parallel"]

[builder]
mode = "dev"
