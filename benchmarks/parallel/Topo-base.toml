[project]
name = "parallel_runtime"

[topo]
root = "topo/main.topo"

[build]
sources = ["src/*.cpp"]
include = ["include", "../../../topo-lang-cpp/runtime/include", "../../runtime/include"]
standard = "c++17"
output = "parallel_base"
link_libs = ["topo-parallel"]

[parallel]
mode = "off"
