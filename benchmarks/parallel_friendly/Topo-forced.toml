[project]
name = "parallel_friendly"

[topo]
root = "topo/main.topo"

[build]
sources = ["src/*.cpp"]
include = ["../../../topo-lang-cpp/runtime/include", "../../runtime/include"]
standard = "c++17"
output = "parallel_friendly_forced"
link_libs = ["topo-parallel"]

[parallel]
mode = "force"
