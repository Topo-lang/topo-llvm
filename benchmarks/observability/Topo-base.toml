[project]
name = "observability"

[topo]
root = "topo/main.topo"

[build]
sources = ["src/*.cpp"]
include = ["include", "../../../topo-lang-cpp/runtime/include", "../../runtime/include"]
standard = "c++17"
output = "observability_base"
link_libs = ["topo-observe"]

[observability]
mode = "off"
